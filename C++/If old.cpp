#include<stdio.h>
#include<conio.h>
 int main()
 {
 	//finding the the old of student;
 	int K,Y,old;
 	printf("enter the old of K= ");
 	scanf("%d",&K);
 	printf("enter the old of Y= ");
 	scanf("%d",&Y);
 	if(K,Y<18);
 	{
 		printf("You can't drink beer");
	 }
	 getch();
 }
